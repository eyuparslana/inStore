push:
  branches:
    - master

name: Test

jobs:
  test:
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-go@v2
        with:
          go-version: '1.16'
      - run: go test ./handler